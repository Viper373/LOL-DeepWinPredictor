name: 更新数据集

on:
  schedule:
    - cron: '0 0 * * 0'  # 每周日 0点运行

jobs:
  run-main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 运行 main.py
        run: |
          python main.py
